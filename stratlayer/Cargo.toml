[package]
name = "stratlayer"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.29", features = ["mman", "fs", "socket", "user", "uio"] }
libc = "0.2"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
strip = true
