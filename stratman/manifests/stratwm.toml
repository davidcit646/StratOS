name = "stratwm"
exec = "/bin/stratwm"
restart = "on-failure"
depends = ["seatd"]
socket = "/run/wayland-0"
socket_timeout_ms = 10000
env = ["LIBSEAT_BACKEND=seatd", "SEATD_SOCK=/run/seatd.sock", "XDG_RUNTIME_DIR=/run"]
namespace = "readonly-user"
