[package]
name = "stratmon"
version = "0.1.0"
edition = "2021"

[dependencies]
stratsup = { path = "../stratsup" }

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
strip = true
