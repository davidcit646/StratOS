[package]
name = "stratterm"
version = "0.1.0"
edition = "2021"

[dependencies]
stratlayer = { path = "../stratlayer" }
nix = { version = "0.29", features = ["mman", "fs", "socket", "user", "term", "process", "poll"] }
libc = "0.2"

[[bin]]
name = "stratterm"
path = "src/main.rs"
