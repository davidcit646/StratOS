[package]
name = "stratterm"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = { package = "gtk4", version = "0.11" }
vte4 = "0.10"
rusqlite = { version = "0.32", features = ["bundled"] }

[profile.release]
opt-level = 2
lto = "thin"
codegen-units = 1
strip = true
