# StratOS Wi-Fi — installed as /config/strat/wpa_supplicant.conf (persisted on CONFIG).
# Uncomment and edit one network block, or add more. strat-wpa starts only when a network={} block exists.

ctrl_interface=/run/wpa_supplicant
update_config=1

# network={
#   ssid="your-ssid"
#   psk="your-passphrase"
# }
